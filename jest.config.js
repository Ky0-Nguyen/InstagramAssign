module.exports = {
  preset: 'react-native',
  modulePathIgnorePatterns: ['<rootDir>/node_modules/'],

  // Các tệp hoặc thư mục không cần biến đổi
  transformIgnorePatterns: ['<rootDir>/node_modules/'],
};
